{
  "name": "flowcast",
  "private": true,
  "workspaces": [
    "./backend/",
    "./client/",
    "./infra/"
  ],
  "scripts": {
    "prepare": "yarn workspaces foreach -A run install",
    "synth": "run prepare && yarn workspace @flowcast/infra run synth",
    "deploy": "run prepare && yarn workspace @flowcast/infra run deploy"
  },
  "packageManager": "yarn@4.0.2",
  "dependencies": {
    "react-text-reveal": "^1.1.2"
  }
}
